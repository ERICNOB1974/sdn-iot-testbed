experiment:
  id: exp008
  name: dijkstra-iinet

environment:
  controller: ryu-4.34-py38
  mininet: mininet-2.3.0
  openflow: "1.3"

controller:
  app: controller.routing_controller
  ip: 127.0.0.1
  port: 6633

routing:
  name: dijkstra
  module: controller.routing.dijkstra
  class: DijkstraRouting
  metric: routing_cost

topology:
  definition: topologies/definitions/iinet.yaml
  placement: topologies/placements/iinet_4_hosts.yaml

network:
  profile: network/profiles/iinet_equal.yaml

traffic:
  profile: traffic/profiles/ping_iinet.yaml
  generator:
    module: traffic.generators.ping
    function: run_ping

collectors:
  - name: flows
    module: collectors.flows
    function: collect_flows
